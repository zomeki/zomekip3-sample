<%= form_tag url_for(action: :index), method: :get, class: 'search' do |f| %>
<table>
  <tr>
    <th>ユーザーID</th>
    <th></th>
  </tr>
  <tr>
    <td><%= text_field_tag 'criteria[account]', params.dig(:criteria, :account), style: 'width: 300px;' %></td>
    <td class="submitters">
      <%= submit_tag '検索', name: :search %>
      <%= submit_tag 'リセット', name: :reset %>
    </td>
  </tr>
</table>
<% end %>
